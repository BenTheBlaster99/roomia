# Roomia — Remaining Tests & Finish Checklist
# Saved aside while building Feature B (Studio photorealistic render).
# Date context: July 2026

---

## Paused testing (finish after Feature B / A)

Three local tests were in progress. Resume these before AI backend public deploy.

### Task 1 — Sarah floor plan workflow
Goal: AI extracts shape only → user clicks walls → enters lengths in metres → Studio.

| Check | Status |
|-------|--------|
| Upload on `/configure` → redirect `/plan?dimensionWalls=1` | Working |
| Click walls / doors / windows | Working |
| Enter wall length → scale plan | Working |
| Open Studio with correct dims | Partially tested |
| Scan matches original floor plan | FAIL — Gemini guesses layout, not true wall tracing |

Known bugs fixed during testing:
- "Gate of heaven" door: metre-sized door on normalized 0–1 plan → clamp + relative openings while `needsWallDimensions`

Deferred accuracy work (do NOT block Tasks 2–3):
- Prefer real CAD (DXF/DWG) upload + parse
- Or OpenCV image → vector → same floor-plan model
- Gemini on JPG alone is not enough for furnished marketing plans
- Clean B&W line drawings work better but still approximate

Decision for MVP: mark Task 1 as **workflow pass / scan accuracy fail**.
Product path later: CAD primary, photo scan secondary.

### Task 2 — Room capture depth
Goal: `/room-capture` → 1–3 photos → depth map + dims → Open Studio.

| Check | Status |
|-------|--------|
| Backend `/depth` multi-photo | Built + automated smoke OK |
| UI upload / depth preview / editable dims | Built, needs manual test |
| Open Studio with query params | Built, needs manual test |

Needs: AI backend on `:8000`, frontend on `:3000`.

### Task 3 — IP-Adapter
Goal: product reference photo guides furniture replacement.

| Check | Status |
|-------|--------|
| Endpoint `/inpaint/replace-ip` | Built |
| Script `roomia-ai-backend/test_ip_adapter.py` | Built |
| Real room + Sarah product photo test | NOT run yet |
| `/photo-studio` UI with reference | Sanity check pending |

---

## After all 3 tests pass

1. Deploy `roomia-ai-backend` to GPU server (e.g. `ai.room-ia.com`)
2. Set Vercel `NEXT_PUBLIC_AI_BACKEND_URL`
3. Update backend CORS for `room-ia.com`
4. Redeploy frontend
5. Quick production smoke: `/room-capture`, `/photo-studio`, `/configure` → `/plan`

---

## New features from PHASE 2 discussion (Sarah + friend)

### Feature B — Studio Photorealistic Render  ← building now
Studio canvas screenshot → SD img2img → photoreal photo → download.
Higher B2B impact, smaller build.

### Feature A — 2D Room Composer  ← built
`/room-composer` + `POST /compose/room`
Upload photo → tips → tap ≤3 zones → catalog pick → sequential IP-Adapter → 3 variations → pick/download.
Test with 1 zone first (faster), then 2–3.

### Explicitly NOT now
- 30s POV walkthrough video (models not ready)
- Capacitor mobile wrap (later, after 2D flow works on mobile browser)
- CAD floor-plan pipeline (separate accuracy track)

---

## Servers for local testing

```bash
# Terminal 1 — AI backend
cd roomia-ai-backend && source venv/bin/activate
uvicorn main:app --host 0.0.0.0 --port 8000

# Terminal 2 — frontend
cd roomia && npm run dev
# Use http://localhost:3000 (kill duplicate: kill $(lsof -t -i:3000))
```

Env: `NEXT_PUBLIC_AI_BACKEND_URL=http://localhost:8000` in `.env.local`
